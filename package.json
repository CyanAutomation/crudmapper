{
  "name": "crudmapper",
  "version": "1.0.0",
  "description": "Map and visualize CRUD permissions across role definitions",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "type-check": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "dev": "tsc --watch"
  },
  "keywords": ["crud", "permissions", "roles", "mapping"],
  "devDependencies": {
    "@types/node": "^20.0.0",
    "jsdom": "^23.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.0.4"
  }
}
